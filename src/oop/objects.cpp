#include "researchinstitute.h"

using namespace std;

class objects {       // The class
public:             // Access specifier
    int myNum;        // Attribute (int variable)
    string myString;  // Attribute (string variable)
    int helloWorld(int abc);

   

};
